import { LightningElement } from 'lwc';
import SVG_LOGO from '@salesforce/resourceUrl/EsstudiaPartner';
export default class ClbinsPartners extends LightningElement {
  EsstudiaPartner = `${SVG_LOGO}#EsstudiaPartner`
 }